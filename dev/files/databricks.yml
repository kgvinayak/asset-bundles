# This is a Databricks asset bundle definition for assetbundleslearn.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: assetbundleslearn
  uuid: 5470c2ef-914a-4bee-975b-2d85b3543cd1

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: "dev_${workspace.current_user.short_name}"
      source_linked_deployment: false
    workspace:
      host: https://dbc-149e7c2c-a2d2.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-149e7c2c-a2d2.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/vinayakrulzdworld@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vinayakrulzdworld@gmail.com/prod/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
